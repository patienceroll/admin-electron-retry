import styled from "styled-components";
import React from "react";

import PageWrapper from "src/framework/component/page-wrapper";

function Edit() {
  return <PageWrapper>edit</PageWrapper>;
}

export default styled(Edit)``;
