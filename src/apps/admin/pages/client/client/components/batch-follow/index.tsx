import { Modal } from "antd";
import React, { forwardRef } from "react";
import styled from "styled-components";

const BatchFollow = forwardRef(function () {
  return <Modal></Modal>;
});

export default styled(BatchFollow)``;
