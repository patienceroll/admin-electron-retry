import styled from "styled-components";
import React from "react";
import PageWrapper from "src/framework/component/page-wrapper";

function Detail() {
  return <PageWrapper>Detail</PageWrapper>;
}

export default styled(Detail)``;
