import { Modal } from "antd";
import React, { forwardRef } from "react";
import styled from "styled-components";

const ChooseAddress = forwardRef(function () {
  return <Modal></Modal>;
});

export default styled(ChooseAddress)``;
